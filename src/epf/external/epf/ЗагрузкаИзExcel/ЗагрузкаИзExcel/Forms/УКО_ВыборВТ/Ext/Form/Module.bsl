
&НаКлиенте
Процедура Выбрать(Команда)
	Закрыть(ИмяВременнойТаблицыУКО)
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Для Каждого СтрокаСписок Из Параметры.СписокЗначений Цикл
		Элементы.ИмяВременнойТаблицыУКО.СписокВыбора.Добавить(СтрокаСписок.Значение, СтрокаСписок.Представление)
	КонецЦикла;
	
	ЭтаФорма.Заголовок = Параметры.Заголовок;
	ЭтаФорма.АвтоЗаголовок = Ложь;
	
КонецПроцедуры
