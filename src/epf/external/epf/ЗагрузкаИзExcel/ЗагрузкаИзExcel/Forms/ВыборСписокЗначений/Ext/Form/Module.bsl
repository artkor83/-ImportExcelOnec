
&НаКлиенте
Процедура Выбрать(Команда)
	
	Закрыть(СписокЗначений);
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Для Каждого СтрокаСписок Из Параметры.СписокЗначений Цикл
		СписокЗначений.Добавить(СтрокаСписок.Значение, СтрокаСписок.Представление, СтрокаСписок.Пометка)
	КонецЦикла;
	
	ЭтаФорма.Заголовок = Параметры.Заголовок;
	ЭтаФорма.АвтоЗаголовок = Ложь;
	
КонецПроцедуры
